{
  "runners": { "process": { "allowedTools": ["node"] } },
  "tasks": [
    { "name": "ok", "runner": "process", "schedules": [{ "interval": "every 1 minute" }], "config": { "command": ["node", "worker.mjs"] } }
  ]
}
